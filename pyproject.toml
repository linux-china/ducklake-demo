[project]
name = "ducklake-demo"
version = "0.1.0"
description = "DuckLake demo with PostgreSQL and MinIO"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.5.4",
]

[dependency-groups]
dev = [
    "pip>=26.1",
    "pytest>=9.1",
    "setuptools>=82.0",
]
